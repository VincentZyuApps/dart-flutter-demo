name: desktop_integration_vincentzyu
description: Linux desktop integration helpers - single instance activation over D-Bus and an MPRIS now playing bridge.
version: 0.1.0
publish_to: none
repository: https://github.com/VincentZyuApps/dart-flutter-demo/tree/main/packages/desktop_integration_vincentzyu

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dbus: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
